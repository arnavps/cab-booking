{
    "name": "uber-clone",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev || (npm run dev --workspace=apps/web & npm run dev --workspace=apps/server)",
        "build": "turbo run build",
        "lint": "turbo run lint"
    },
    "devDependencies": {
        "turbo": "latest"
    }
}